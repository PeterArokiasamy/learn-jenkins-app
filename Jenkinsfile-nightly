pipeline {
    agent any

    stages {

        stage('Build Docker image') {
            steps {
                /* To Execute main Docker 
                    sh 'docker build -t my-playwright .' */
                sh 'docker build -f ci/Dockerfile -t my-playwright .'
            }
        }
    }
}